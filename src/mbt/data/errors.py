"""数据层的错误类型。

缺口纪律（ADR-0005）：缺失跳过、**异常报错**、永不填充。这里承载的是后者——
「文件内容与预期不符，不能继续」，而不是「这个标的今天没有数据」（后者是正常的
停牌状态，只表现为没有记录）。
"""


class MarketDataError(Exception):
    """行情数据不可信。

    异常在这里意味着「文件内容与预期不符，不能继续」，而不是「这个标的今天没有数据」
    ——后者是正常状态（停牌），只表现为没有记录。
    """
